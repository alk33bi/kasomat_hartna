# خصومات حارتنا

A Vite + React + TypeScript application built with shadcn-ui and Tailwind CSS.

## Project Overview

This is a clean, independent React/Vite project for discovering local deals and discounts.

## Technologies Used

- **Vite** - Fast build tool and dev server
- **React 18** - UI library
- **TypeScript** - Type-safe JavaScript
- **Tailwind CSS** - Utility-first CSS framework
- **shadcn-ui** - Beautiful, accessible UI components
- **React Router** - Client-side routing
- **React Hook Form** - Efficient form handling
- **Framer Motion** - Smooth animations

## Getting Started

### Prerequisites

- Node.js 18+ and npm

### Installation

1. Clone the repository:
```sh
git clone <YOUR_GIT_URL>
cd kasomathartna-main
```

2. Install dependencies:
```sh
npm install
```

### Development

Start the development server with hot module reloading:

```sh
npm run dev
```

The application will be available at `http://localhost:8080`

### Building

Build the project for production:

```sh
npm run build
```

### Preview

Preview the production build locally:

```sh
npm run preview
```

### Linting

Check and fix code style issues:

```sh
npm run lint
```

### Testing

Run tests:

```sh
npm run test
```

Watch mode for development:

```sh
npm run test:watch
```

## Project Structure

```
src/
├── components/       # Reusable React components
├── pages/           # Page components for routes
├── hooks/           # Custom React hooks
├── lib/             # Utility functions
├── data/            # Mock data and constants
├── types/           # TypeScript type definitions
├── assets/          # Static assets (images, etc.)
└── [main files]     # App.tsx, main.tsx, etc.
```

## Available Scripts

- `npm run dev` - Start development server
- `npm run build` - Build for production
- `npm run build:dev` - Build in development mode
- `npm run preview` - Preview production build
- `npm run lint` - Run ESLint
- `npm run test` - Run tests once
- `npm run test:watch` - Run tests in watch mode

## License

MIT
